# JSVOC 项目交接文档

更新时间：2026-05-19
当前分支：`sp8-engineering`
最新提交：`9e8ba87 feat: enrich media generation workflow`
线上地址：`https://jsvoc.szkakayiduo.com`
服务器：`47.120.41.143`，项目目录：`/opt/JSVOC`

## 1. 当前状态

- 代码已提交并推送到 GitHub：`origin/sp8-engineering`
- 服务器已部署最新提交：`9e8ba87`
- Docker 服务状态已验证：
  - `jsvoc_backend` healthy
  - `jsvoc_frontend` running
  - `jsvoc_postgres` healthy
- 线上检查：
  - 后端健康检查：`http://127.0.0.1:8000/health` 返回 `200`
  - 前端项目页：`https://jsvoc.szkakayiduo.com/projects` 返回 `200`
- 数据库迁移已到最新：`20260518_0010`
- 本地验证：
  - 后端：`python -m unittest discover -s tests`，93 个测试通过
  - 前端：`npm run build`，通过

## 2. 本轮已完成

### 积分系统

- 新增积分账户和积分流水：
  - `credit_accounts`
  - `credit_transactions`
- 用户注册后自动赠送 `2000` 积分，等价 `20` 元。
- 标准购买比例：`10000` 积分 = `100` 元。
- 生图扣费：每张 `20` 积分。
- 生视频按 `1 元 = 100 积分`，并向上取整到 10 的倍数。
- 关键文件：
  - `backend/app/models/credit.py`
  - `backend/app/services/credit_service.py`
  - `backend/app/api/credits.py`
  - `frontend-v2/src/lib/api/credits.ts`

### 生图工作流

- 已支持文生图、图生图。
- 图生图支持人设、货品、场景三类参考图。
- 参考图上传会持久化，不再只存在前端内存。
- 后端会压缩参考图，解决图片过大导致 provider 返回 `413 Request Entity Too Large` 的问题。
- 文生图新增“提示词优化”能力，用于过滤项目名、人名、感染因素等无关信息，并重写成更适合生成图像的提示词。
- 生图完成后会写入数字资产。
- 关键文件：
  - `backend/app/api/image_generation.py`
  - `backend/app/services/image_generation_service.py`
  - `backend/app/services/image_prompt_enhancement_service.py`
  - `backend/app/api/reference_images.py`
  - `backend/app/services/project_reference_image_service.py`
  - `frontend-v2/src/app/projects/[id]/images/page.tsx`

### 生视频工作流

- 生视频 API 已切到火山 Ark 官方任务接口：
  - Base URL：`https://ark.cn-beijing.volces.com`
  - 任务创建路径：`/api/v3/contents/generations/tasks`
- 环境变量：
  - `ARK_API_KEY`
  - `VIDEO_GENERATION_API_KEY`
  - `VIDEO_GENERATION_BASE_URL`
- 不要把 API Key 写进代码或文档。
- 旧的 `ep-m-...` endpoint 已取消兼容，后端会拒绝这类错误模型，避免误提交和误扣费。
- 当前可用模型只保留官方模型名：
  - Seedance 2.0：`doubao-seedance-2-0-260128`
  - Seedance 2.0 Fast：`doubao-seedance-2-0-fast-260128`
- 关键文件：
  - `backend/app/api/video_generation.py`
  - `backend/app/services/video_generation_service.py`
  - `backend/app/services/video_model_catalog.py`
  - `frontend-v2/src/app/projects/[id]/videos/page.tsx`

### 视频价格

- Seedance 2.0：
  - `480p / 15s`：700 积分
  - `720p / 15s`：1500 积分
  - `1080p / 15s`：3700 积分
- Seedance 2.0 Fast：
  - `480p / 15s`：560 积分
  - `720p / 15s`：1200 积分
  - 不支持 `1080p`
- Fast 模型如果选择 `1080p`，前端和后端都应阻止。

### UI 与入口

- 品牌名已改为：`短视频运营中心`
- 英文名已改为：`Operation Center`
- 图标已改为扁平化平安扣方向。
- 顶部导航已增加：
  - 生图
  - 生视频
- 工作流导航已放到左侧，鼠标悬停会高亮。
- 主色已调整：帝王绿更深，阳绿更亮，苹果绿更浅一层。
- 生图/生视频上传图片已显示“图片1、图片2...”。
- 右上角删除按钮已改为半透明扁平化小图标，平时隐藏，悬停/经过附近显示。

### 提示词入口联动

- 从项目进入生图：项目提示词写入提示词框。
- 从选题进入生图：选题提示词写入提示词框。
- 从文案进入生图：文案提示词写入提示词框。
- 从首页入口进入：提示词框为空。
- 所有入口带入的提示词都可以继续编辑、优化、删除。

## 3. 重要路由

### 前端页面

- `/projects`
- `/projects/[id]`
- `/projects/[id]/images`
- `/projects/[id]/videos`
- `/images`
- `/videos`

### 后端接口

- 生图：`POST /api/creation/images/generate`
- 图片任务查询：`GET /api/creation/images/tasks/{task_id}`
- 提示词优化：`POST /api/creation/images/enhance-prompt`
- 视频模型列表：`GET /api/creation/videos/models`
- 生视频：`POST /api/creation/videos/generate`
- 视频任务查询：`GET /api/creation/videos/tasks/{task_id}`
- 积分账户：`GET /api/credits/account`
- 积分流水：`GET /api/credits/transactions`
- 数字资产：`GET /api/digital-assets`

## 4. 部署命令

服务器上执行：

```bash
cd /opt/JSVOC
git fetch origin
git reset --hard origin/sp8-engineering
docker compose --progress plain up -d --build
docker compose ps
curl -sS http://127.0.0.1:8000/health
curl -sS http://127.0.0.1:5173/projects
```

部署前如有正在生成的任务，先不要重启后端：

```bash
cd /opt/JSVOC
docker compose exec -T postgres psql -U postgres -d short_video_ops <<'SQL'
select id, task_type, status, created_at
from generation_tasks
where status in ('queued','running')
order by id desc;
SQL
```

## 5. 本地验证命令

后端：

```bash
cd backend
python -m unittest discover -s tests
```

前端：

```bash
cd frontend-v2
npm run build
```

## 6. 注意事项

- 不要再使用 `ep-m-...` 作为 Seedance 模型配置。
- 火山 Ark 视频生成使用官方模型名，不使用控制台 endpoint id。
- `.env` 不提交，所有密钥只从环境变量读取。
- 线上服务器当前有若干 `.env.bak.*` 备份文件，不属于 Git。
- 本地工作区还有 `.cc-connect` 附件、临时 JSON、cookies、pid、tmp 脚本等未跟踪运行产物，不要提交。
- 如果用户反馈“页面卡住但任务完成”，优先检查任务轮询和 `generation_tasks` 状态，再检查数字资产是否写入。
- 如果用户反馈视频内容和故事板无关，优先确认前端上传的图片/视频/音频是否真的进入 `content` 数组，并确认 role 是否正确。
- 如果用户反馈 480p 生成后变 720p，优先检查前端 quality 入参、后端 normalize、provider payload，以及扣费时读取的最终质量。

## 7. 常用排查

查看容器日志：

```bash
cd /opt/JSVOC
docker compose logs -f backend
docker compose logs -f frontend-v2
```

查看最新任务：

```bash
cd /opt/JSVOC
docker compose exec -T postgres psql -U postgres -d short_video_ops <<'SQL'
select id, task_type, status, provider_task_id, created_at, updated_at
from generation_tasks
order by id desc
limit 20;
SQL
```

查看数字资产：

```bash
cd /opt/JSVOC
docker compose exec -T postgres psql -U postgres -d short_video_ops <<'SQL'
select id, asset_type, project_id, generation_task_id, created_at, access_url
from digital_assets
order by id desc
limit 20;
SQL
```

确认视频模型配置：

```bash
cd /opt/JSVOC
docker compose exec -T backend python - <<'PY'
from app.core.config import get_settings
from app.services.video_model_catalog import video_model_catalog, video_model_availability
s = get_settings()
for m in video_model_catalog(s):
    print(m.key, m.value, m.available)
print(video_model_availability("ep-m-20260415230052-5dwwh", s))
PY
```

## 8. 补充说明

### 积分系统

- 积分不足：拒绝请求，返回错误提示，不允许负数
- 并发扣费：有数据库行锁，防止并发超扣
- 任务失败：积分自动退回

### 视频生成失败

- 前端会显示失败原因
- 用户可重新操作重试

### 前端构建环境

- Node.js：`20.x`（docker-compose 使用 `node:20-alpine`）
- Next.js：`16.2.6`
- React：`19.2.4`
- 不需要 `.env.local`，环境变量通过 docker-compose 传入：
  - `API_BASE_URL=http://backend:8000`（容器内）
  - `NEXT_PUBLIC_API_BASE_URL` 由 `next.config.ts` 默认值处理

### 数据库备份

- PostgreSQL 数据持久化到 Docker volume：`jsvoc_postgres_data`
- 无自动备份脚本，需手动或外部工具备份
- 备份命令示例：
  ```bash
  docker compose exec -T postgres pg_dump -U postgres short_video_ops > backup_$(date +%Y%m%d).sql
  ```

### 监控告警

- 无服务监控，依赖 docker restart policy 自动重启
- 无积分异常告警，需人工排查积分流水

## 9. 下一步建议

- 给生图/生视频任务轮询加更明确的前端状态提示，例如“已提交、排队中、生成中、保存资产中、完成”。
- 给视频生成增加 provider payload 调试日志开关，只在管理员或 DEBUG 环境下开启。
- 把视频生成的参考图片、参考视频、参考音频在任务详情里展示出来，方便判断故事板是否真正传入。
- 给积分流水增加后台管理入口，方便手动补积分和查扣费原因。
